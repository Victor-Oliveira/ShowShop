<p id="notice"><%= notice %></p>

<% @cart.products.each do |product| %>
	<div class="col-md-3">
		<%= link_to image_tag( product.picture, class: 'card-img'), data: { modal: true }, :product_details => true %>
	</div>
<% end %>

<%= link_to 'Edit', edit_cart_path(@cart) %> |
<%= link_to 'Back', carts_path %>
